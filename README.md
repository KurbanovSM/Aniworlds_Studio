# Aniworlds Studio

Отдельная автономная программа для подготовки данных Aniworlds AI. Studio не
подключается к VPS и не содержит адрес сервера: она сохраняет проверенные файлы
локально, после чего их можно перенести на сервер через Cyberduck.

Реализованы два раздела. Генератор промокодов:

- промокод подписки: вручную меняется только общий лимит активаций;
- промокод ходов: задаются ходы, лимит активаций и необязательное окончание;
- код `ANI-XXXX-XXXX` создаётся автоматически;
- каждый результат сохраняется отдельным JSON-файлом с именем кода.

Редактор миров позволяет:

- создавать и открывать локальные черновики вселенной;
- редактировать несколько периодов, лор и начальную ситуацию;
- задавать валюту, название запаса сил и лимиты способностей;
- редактировать полный каталог периодов, всех локаций и связей между ними;
- задавать ровно три стартовых набора на период и до 10 объявленных предметов
  в каждом наборе;
- редактировать виды и расы, языки, объединения, предметы, магазины, подготовленных
  персонажей и NPC;
- задавать персонажам периоды, происхождение, мышление, языки, черты, способности,
  участие и лидерство в объединениях;
- задавать предметам свойства, ограничения, цену, допустимые магазины, вес появления
  и необязательный предел экземпляров на мир;
- проверить и просмотреть JSON до публикации;
- создать локальный пакет `*.world.json` для ручной загрузки в серверную папку
  `worlds` через Cyberduck.

## Запуск

```powershell
py -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e .
.\.venv\Scripts\aniworlds-studio.exe
```

После установки программу можно открывать двойным щелчком по
`start-studio.cmd` в корне проекта.

Промокод загружается в серверный подкаталог `promocodes`, а опубликованная основа
мира версии 2 — в `worlds` общего дерева Studio. Программа не отправляет файлы на
сервер самостоятельно.

## Проверка разработки

```powershell
.\.venv\Scripts\python.exe -m pip install -e ".[dev]"
.\.venv\Scripts\ruff.exe check .
.\.venv\Scripts\pyright.exe
.\.venv\Scripts\python.exe -m pytest --cov
```
